The attached image contains encyclopedia passages rendered as a bitmap: monospace pixel font, {cols} characters per row, {rows} rows, read left-to-right then top-to-bottom. Original paragraph breaks were collapsed to spaces.

Questions follow after the image. Do NOT answer them yet, and do NOT transcribe the image. For each question, skim the image coarsely (keywords, names, numbers) and guess which horizontal band of rows most likely contains the answer.
- Output a numbered list, one line per question, each line exactly of the form `rows A-B` (1-indexed, top row is 1; keep each band at most 25 rows tall).
- No answers, no commentary, nothing else.
