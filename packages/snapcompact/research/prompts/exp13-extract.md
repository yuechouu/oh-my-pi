You are compacting the reference passages above for a later question-answering session. Produce an EXTRACTIVE digest: copy out the most load-bearing sentences and spans VERBATIM.

Rules:
- Copy text exactly as it appears in the passages: no paraphrasing, no summarizing, no rewording, no truncation of sentences. Keep numbers, dates, names, units, capitalization, and spelling character-for-character.
- Preserve the original order of the source; never reorder spans.
- Prefer complete sentences dense with facts: named entities, quantities, dates, definitions, titles, causal claims. Drop filler, transitions, and rhetorical framing.
- Cover the FULL span of the material evenly — do not spend the budget on the earliest passages and starve the later ones.
- Separate non-adjacent extracts with a line containing only: ...
- Hard output budget: at most {budget} characters in total. Use most of it; never exceed it.
- Output ONLY the extracted text (and the ... separators). No headers, no commentary, no explanations.
